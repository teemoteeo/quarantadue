#include <stddef.h>  // for size_t, NULL

void *ft_memchr(const void *s, int c, size_t n) {
  // Scan the first n bytes of s for the byte c; return a pointer to it or NULL
  // Allowed libc: none

  // Implementation goes here
}
