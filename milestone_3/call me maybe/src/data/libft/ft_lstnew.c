#include <stddef.h>  // for size_t/NULL, etc.
#include <stdlib.h>  // for malloc

// Define the struct at the top of the file as 'typedef struct s_list { void *content; struct s_list *next; } t_list;'
// Define the function prototype for ft_lstnew
typedef void* (*ft_lstnew)(void *content);

// Prototype: t_list *ft_lstnew(void *content);
// Description: Return a newly allocated node with the given content and a NULL next pointer. There is no libft.h: define the struct at the top of the file as 'typedef struct s_list { void *content; struct s_list *next; } t_list;'
// Allowed libc: malloc
// Filename: ft_lstnew.c

// Implementation of ft_lstnew
typedef void* (*ft_lstnew)(void *content);

// Function prototype for ft_lstnew
typedef void* (*ft_lstnew)(void *content);

// Implementation of ft_lstnew
typedef void* (*ft_lstnew)(void *content);

// Function prototype for ft_lstnew
typedef void* (*ft_lstnew)(void *content);
